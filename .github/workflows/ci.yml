name: Simple CI Pipeline

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/Checkout@v3

      - name: Run a simple command
        run: echo "hello, This is Harshitha "

      - name: step1
        run: echo"this is step 1 one"

      - name: step2
        run: echo"this is step 2 two"

      

